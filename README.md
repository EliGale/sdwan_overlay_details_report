# sdwan_overlay_details_report
Scans a folder of Cisco SD-WAN **admin-tech bundles** from control plane components (vManage, vSmart, vBond) and prints four summary tables covering device identity, certificates, WAN interfaces, cluster topology, and interface addressing — all from a single command, no external dependencies.
